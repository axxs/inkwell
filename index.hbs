{{!< default}}
<h1 class="sr-only">{{meta_title}}</h1>
<div class="container">
  <div class="post-feed">
    {{#foreach posts}}
      {{> post-card}}
    {{/foreach}}
  </div>
  {{pagination}}
</div>
{{> subscribe-cta}}
